import { Title } from "components/typography";

import "./logo.scss";

const Logo = () => {
  return (
    <Title type="h1" className="logo">
      A Big Title
    </Title>
  );
};

export default Logo;
