import React from "react";

import Header from "./";

export default {
  title: "Atoms/Header",
  component: Header,
};

export const Standard = () => (
  <Header>Some dummy content inside the header.</Header>
);
